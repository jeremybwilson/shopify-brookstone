// Stuck nav shading and coloration

// SHADOW : When stick nav is active
&:hover,
&.isStuck,
&.show-search-bar,
&.mobile-nav-open {
    box-shadow: $navBoxShadow;

    #nav-logo-backing {
        box-shadow: $navBoxShadow;
    }

    &.transparent-landing {
        background: {{ settings.header_background }};

        #nav-bar-bg,
        #nav-user ul#nav-user-list .nav-sites ul.sites-picker,
        #nav-user ul#nav-user-list .nav-sites ul.sites-picker li,
        #nav-user ul#nav-user-list .nav-sites ul.sites-picker li .site-name {
            background: {{ settings.header_background }};
        }

        // Easier to bundle it as an important'd override, then override again elow this in the isStuck block
        .nav-category-header,
        .account-text,
        .icon-cart-text,
        .cart-count,
        .site-name,
        .nav-item svg path,
        .icon-header-logo path {
            color: $navHeaderColor !important; 
            fill: $navHeaderColor !important;
        }

        .nav-wishlist {
            svg path {
                stroke: $navHeaderColor !important;
            }

            .wk-filled svg path {
                fill: $navHeaderColor !important;
                fill-opacity: 1;
            }
        }
            

        .icon-nav-mobile-menu .bar {
            background: $navHeaderColor !important;
        }
    }
}